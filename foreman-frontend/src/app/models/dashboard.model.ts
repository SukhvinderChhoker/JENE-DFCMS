export interface DashboardStats {
  totalCases: number;
  openCases: number;
  closedCases: number;
  totalTasks: number;
  queuedTasks: number;
  completedTasks: number;
  totalEvidence: number;
  activeUsers: number;
}
